<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
     <%= link_to "Post Board", root_path, :class => 'navbar-brand' %>
    </div>
    <ul class="nav navbar-nav">
    <% if logged_in? %>
      <li><%= link_to 'New Post', new_post_path %></li>
      <% end %>
      <li><%= link_to 'Popular'%></li>
      <li><a class="dropdown-toggle" data-toggle="dropdown" href="#">  Category<span class="caret"></span></a>
     
      </li> 
    </ul>
    <ul class="nav navbar-nav navbar-right">
     <% if logged_in? %>
         <li class="dropdown">
            <%= link_to '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown' do %>
            <%= current_user.name %> <span class='caret'></span>
            <% end %>
            <ul class="dropdown-menu">
              <li><%= link_to 'User Profile', user_path(current_user) %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Logout', logout_path %></li>
            </ul>
          </li>
   <% else %>
    <li><%= link_to 'Sign Up', register_path %></li>
     <li><%= link_to 'Login', login_path %></li>
  <% end %>
    </ul>
  </div>
</nav>